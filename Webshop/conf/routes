# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /startsida                      controllers.Application.startsida()
GET     /categorysida					controllers.Application.categorysida()
GET     /produktsida                    controllers.Application.produktsida()
GET     /shoppingvagn					controllers.Application.shoppingvagn()
GET     /checkout						controllers.Application.checkout()
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
